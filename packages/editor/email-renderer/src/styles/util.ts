export const CS_CONTENT_PREFIX = 'csg';
export const createClassName = (name: string) => {
  return `${CS_CONTENT_PREFIX}-${name}`;
};
export const MEDIA_PREVIEW_IMAGE_WIDTH = 200;
export const MEDIA_PREVIEW_IMAGE_HEIGHT = 120;
