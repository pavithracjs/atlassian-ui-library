{
  "name": "generator",
  "version": "1.0.0",
  "description": "Run this to generate static image asset modules. This script converts svgs from core/icons module into base64 encoded png data in their respective modules for use in EMAILS. ",
  "main": "generate.js",
  "author": "",
  "scripts": {
    "start": "ts-node --files ./generate.ts"
  },
  "license": "ISC",
  "dependencies": {
    "rimraf": "^2.6.3",
    "svgexport": "^0.3.2",
    "ts-node": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^12.0.8",
    "@types/rimraf": "^2.0.2"
  }
}
