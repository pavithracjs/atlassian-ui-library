import { dark, light } from './modes';
import { AppNavigationTheme } from './types';

export const darkTheme: AppNavigationTheme = { mode: dark };
export const lightTheme: AppNavigationTheme = { mode: light };

export const defaultTheme: AppNavigationTheme = lightTheme;
